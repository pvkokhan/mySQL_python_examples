import createtable
import read
import connect
import update

#connection = connect.create_connection("localhost", "root", "Fedotdanet0t", "sm_app")

#createdb.create_database(createdb.connection, createdb.create_database_query)

#createtable.execute_query(createtable.connection, createtable.create_user_table)

#createtable.execute_query(createtable.connection, createtable.create_posts_table)

#createtable.execute_query(createtable.connection, createtable.create_comments_table)

#createtable.execute_query(createtable.connection, createtable.create_likes_table)

#createtable.execute_query(createtable.connection, createtable.create_users)

#createtable.execute_query(createtable.connection, createtable.create_posts)

#createtable.execute_query(createtable.connection, createtable.create_comments)

#createtable.execute_query(createtable.connection, createtable.create_likes)

#read.execute_read_query(connect.connection, read.select_posts_comments_users)

